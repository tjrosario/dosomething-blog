box: wercker/php
# Build definition
deploy:
  steps:
    - script:
        name: git push code to Pantheon
        code: git push ssh://$GIT_REMOTE_USER@$HOSTNAME:$REPOSITORY
#   after-steps:
#     - hipchat-notify:
#         token: $HIPCHAT_TOKEN
#         room-id: $HIPCHAT_ROOM_ID
#         from-name: wercker