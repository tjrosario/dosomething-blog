box: wercker/php
# Build definition
deploy:
  steps:
    - add-to-known_hosts:
        hostname: $HOSTNAME
        fingerprint: $FINGERPRINT
    - blisteringherb/git-push@0.0.4
#   after-steps:
#     - hipchat-notify:
#         token: $HIPCHAT_TOKEN
#         room-id: $HIPCHAT_ROOM_ID
#         from-name: wercker