box: wercker/php
# Build definition
deploy:
  steps:
    # - add-to-known_hosts:
    #     hostname: ENV["HOSTNAME"]
    #     fingerprint: ENV["FINGERPRINT"]
    - leipert/git-push@0.4.4:
        host: ENV["HOSTNAME"]
        repo: ENV["REPOSITORY"]
        branch: master
#   after-steps:
#     - hipchat-notify:
#         token: $HIPCHAT_TOKEN
#         room-id: 710526
#         from-name: wercker