box: wercker/php
# Build definition
deploy:
  steps:
    - script:
        name: git push code to Pantheon
        code: git push ENV["GIT_REMOTE_USER"]@ENV["HOSTNAME"]:ENV["REPOSITORY"]
#   after-steps:
#     - hipchat-notify:
#         token: $HIPCHAT_TOKEN
#         room-id: 710526
#         from-name: wercker